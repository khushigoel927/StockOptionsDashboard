# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# App runtime artifacts
.app.pid
.app-*.pid
app.log
app-*.log

# Streamlit Cloud secrets — never commit
.streamlit/secrets.toml

# Paper trading book — local state only. The deployed app never writes one:
# every visitor's book lives in their session (see core/stores.py).
paper_book.json
paper_book.json.tmp
paper_book.corrupt-*.json
paper_book.backup-*.json

# Tooling / editors
.claude/
.agents/
.DS_Store
.idea/
.vscode/
